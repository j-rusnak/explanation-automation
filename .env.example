# techshort does not require an API key. The Codex provider uses existing CLI authentication.
TECHSHORT_PROJECTS_ROOT=projects
TECHSHORT_REVIEWER_ID=local-reviewer
# Local neural synthesis uses this Git-ignored cache after explicit `techshort audio setup --yes`.
TECHSHORT_KOKORO_CACHE=.techshort/models/kokoro
# Windows System.Speech remains an offline fallback. No TTS key is read.
# Optional official TikTok draft transfer. Create an approved TikTok developer app with the
# `video.upload` scope, then set this token/OpenID pair from the same OAuth response only in the
# current local environment. Keep these tracked
# example values blank:
# techshort never stores or logs the values, and it never accepts browser cookies or sessions.
TECHSHORT_TIKTOK_ACCESS_TOKEN=
TECHSHORT_TIKTOK_OPEN_ID=
# Optional local-only transcription. The model must already exist; techshort never downloads one.
TECHSHORT_WHISPER_CLI=
TECHSHORT_WHISPER_MODEL=
