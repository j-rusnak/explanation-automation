# Security and threat model

Sources, filenames, manifests, and provider output are hostile data. techshort never follows embedded links, executes attachments/code blocks, dynamically imports project data, or treats source instructions as commands. Prompts explicitly delimit excerpts as untrusted quoted data.

| Threat                                                   | Mitigation                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Malformed, encrypted, scanned, huge, or compressed PDFs  | pypdf strict mode; encryption rejection; 25 MiB and 250-page input limits; cumulative 32 MiB extracted/raw-output limit; OCR-required failure; hashed and strictly revalidated page/section metadata                                                                                                                                                                |
| Prompt injection requesting filesystem/network access    | excerpt-only prompt context; explicit injection warning; Codex ephemeral/read-only mode; ignored user rules/config when supported; strict schema validation                                                                                                                                                                                                         |
| Hostile filenames and path traversal                     | basename sanitization, slug allowlist, canonical containment, project-relative path validators                                                                                                                                                                                                                                                                      |
| SSRF                                                     | URL ingestion is absent; any future implementation must reject private/loopback/link-local targets and revalidate every redirect                                                                                                                                                                                                                                    |
| Arbitrary code, shell injection, hostile HTML/SVG        | no `eval`, `exec`, or dynamic imports; fixed executables and argument arrays; allowlisted primitives; inert-text checks in both Python and TypeScript; escaped companion HTML                                                                                                                                                                                       |
| Secret leakage                                           | no API key required; no auth-file inspection; sanitized provider environment; no full-document logging                                                                                                                                                                                                                                                              |
| Stale or forged approvals                                | exact content/evidence/audio/transcript/timing hashes, immutable version history, append-only review invalidations, explicit angle selection, configuration/provenance-bound final approval; audio, transcript, synthesis, or timing changes invalidate affected final review                                                                                       |
| Fabricated numbers, units, citations, or evidence labels | deterministic assertion-token checks during generation, approval, and QA; scene citations/labels and SourceReceipt IDs must resolve through approved claims                                                                                                                                                                                                         |
| Rights mistakes and source redistribution                | explicit asset rights and embedding status; concrete permissive-license metadata; unknown/restricted/citation-only blockers; short excerpts only                                                                                                                                                                                                                    |
| Model substitution or unexpected network access          | Kokoro setup alone permits remote model access and pins one model ID and immutable revision; synthesis forces local-only loading from the explicit Git-ignored cache. The runtime is pinned by `package-lock.json`, uses `q8` on CPU, rejects cache symlinks, requires an ONNX file, and reports per-file plus aggregate hashes for review                          |
| Local synthetic narration abuse or secret-bearing text   | Kokoro accepts exact-key versioned JSON with an allowlisted stock voice, bounded speed, stable segment IDs, at most 100 segments, and a 128 KiB text limit. System.Speech accepts only the current approved script through a fixed helper. Neither path accepts SSML, arbitrary commands, voice-cloning samples, credentials, or provider-selected executable paths |
| Platform credential or account leakage                   | TikTok access token and OpenID are injected only through the current process environment; tokens are redacted and never persisted or logged; upload records keep only the account-subject SHA-256 and an inert operator label; fixed official HTTPS hosts are allowlisted; cookies, browser profiles, and account sessions are rejected                                                                   |
| Viewer privacy leakage                                   | experiment imports accept an allowlisted aggregate schema only; person-level fields and unknown columns are rejected; post references identify content, not viewers                                                                                                                                                                                                 |
| Fabricated or selectively timed performance data         | no metric synthesis or API scraping; templates demand actual cumulative snapshots; counts cannot regress; observation windows and current variant hashes are validated; recommendations preserve uncertainty. techshort cannot authenticate manually entered platform totals, so honest operator review remains required                                            |
| Accidental or unauthorized transfer or publication       | package consent does not authorize API transfer; a second exact-phrase decision binds one current package/video and TikTok account before the attempt is durably recorded; the allowlisted API operation creates a draft only and never directly publishes, schedules, or chooses visibility                                                                                  |
| Duplicate upload after an uncertain response             | attempts and status transitions are append-only; one immutable package permits one attempt; timeouts, disconnects, malformed responses, and other uncertain outcomes become `ambiguous` receipts; techshort never re-uploads them automatically, and the operator must inspect TikTok before approving a newly reviewed package and intent                                    |
| Draft/package mismatch or stale approval                 | preflight re-hashes the immutable package and checks its current export, experiment, variant, evidence, claims, limitation, rights, media, captions, and cover dependencies; any changed or missing byte blocks transfer; the separate cover PNG is never represented as transferred by the API                                                                                |

Subprocesses use argument arrays, `shell=False`, timeouts, and bounded output. Normal tests have no network or live-model calls. The optional Kokoro model download is deliberately excluded from normal setup and tests; it occurs only when the operator runs the explicit one-time `setup` action. Subsequent `synthesize` actions set remote model access to false.

The optional TikTok client is not a general HTTP client. It uses the official Content Posting API
draft-upload and status endpoints, accepts only the `video.upload` workflow, and uploads the
reviewed MP4 as one bounded file of at most 64,000,000 bytes. Direct Post, `video.publish`, public-visibility controls,
background scheduling, browser automation, cookie/session reuse, and analytics access are absent.
Provider error text and diagnostics are treated as untrusted and redacted before display. A live
transfer requires an operator-created TikTok developer app and OAuth grant; no live upload occurs
during setup or normal tests.

The cache hash report makes local changes visible and bindable, but a hash generated after a
compromise is not independent proof of model authenticity. Protect `.techshort/models/kokoro` as
a local dependency cache, compare unexpected identity changes before use, and rerun setup from the
pinned lockfile and revision on a trusted connection when integrity is in doubt. The cache is
ignored by Git and must not be copied into exports.

Organic observations are still untrusted local input. JSON/CSV imports are size- and row-bounded,
strictly reject extra fields, require timezone-aware ordered windows, and are stored atomically
under the active project. New snapshots stale prior recommendations. Experiment analysis re-hashes
the approved video and cover before producing or approving a recommendation, so changed media
cannot silently inherit an earlier result.

The PDF parser still runs in-process. Byte/page/output limits reduce common resource-exhaustion risks but are not a full memory-isolation boundary for a novel parser exploit; high-risk PDFs should be pre-screened in a disposable environment.
